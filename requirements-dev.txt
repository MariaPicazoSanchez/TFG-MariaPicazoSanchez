# Dependencias de desarrollo (no se incluyen en el instalador)
# Instalar con: pip install -r requirements-dev.txt

# Core de la app que usan los tests
pandas==2.2.3
openpyxl==3.1.5
xlrd==2.0.2
streamlit==1.47.1
geopy==2.4.1

# Testing
pytest>=9.0

# Linting
ruff>=0.4
