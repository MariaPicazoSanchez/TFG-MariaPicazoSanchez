# Dependencias de desarrollo (no se incluyen en el instalador)
# Instalar con: pip install -r requirements-dev.txt

# Core de la app (mismo conjunto que install_root/requirements.txt)
-r install_root/requirements.txt

# Testing
pytest>=9.0,<10.0
pytest-cov>=5.0,<7.0

# Linting
ruff>=0.4,<1.0
